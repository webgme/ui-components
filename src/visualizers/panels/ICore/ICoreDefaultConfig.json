{
  "templates": {
    "coreTraverse": {
      "displayName": "Traverse Model",
      "description": "Illustrates how to use core.traverse",
      "script": "// Traverse model",
      "default": false
    },
    "blobClient": {
      "displayName": "Storing Files",
      "description": "Shows how to use the blob-client and link to the artifacts in the result.",
      "script": "// Blob client",
      "default": false
    },
    "activeNode": {
      "displayName": "Log name attribute",
      "description": "Minimal example logging the name of the active node.",
      "script": "function (callback) {\n \tvar activeNode = this.activeNode,\n      core = this.core,\n      logger = this.logger;\n  \n  logger.info('Current node name:', core.getAttribute(activeNode, 'name'));\n  callback();\n}",
      "default": true
    }
  },
  "consoleWindow": {
    "verticalOrientation": true,
    "space": "50%",
    "logLevel": "debug",
    "clearBeforeExecution": true
  },
  "codeEditor": {
    "autoSave": false,
    "theme": null
  }
}